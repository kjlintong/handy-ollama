# Ollama 模型管理与自定义 - Modelfile 高级编写

> **版本基线**：Ollama 0.17.x
> **本章定位**：掌握 Modelfile 编写，把「Agent 场景需要的系统提示词、采样参数、工具模板」固化成专属模型
> **前置**：[1. 自定义导入模型](./1.%20自定义导入模型.md)、[3. GGUF 深度解析与量化选型](./4.%20GGUF%20深度解析与量化选型.md)

## 一、Modelfile 是什么

Modelfile 是 Ollama 的**模型构建文件**，类似 Docker 的 Dockerfile：用几条指令描述「基于什么模型、加什么系统提示词、设什么参数」，然后 `ollama create` 一条命令构建出**专属模型**。

```
FROM qwen3:8b                    # 基础模型
SYSTEM "你是一个严谨的本地 Agent 助手……"  # 固化系统提示词
PARAMETER temperature 0.2        # 固化采样参数
```

构建出的模型独立命名、独立管理（`ollama list` / `ollama rm` 可见），与基础模型互不影响。**这是 Agent 场景的常用工程实践**：团队共享同一个"调好参的 Agent 模型"，而不是每次对话都手动传 system prompt。

## 二、指令速查

| 指令 | 作用 | 必填 |
|---|---|---|
| `FROM` | 指定基础模型（模型名 / GGUF 文件 / Safetensors 目录） | ✅ |
| `PARAMETER` | 设置运行参数（temperature、num_ctx、num_gpu 等） | |
| `SYSTEM` | 设置系统提示词（角色、行为规则） | |
| `TEMPLATE` | 自定义发送给模型的完整提示词模板（高级） | |
| `ADAPTER` | 应用 (Q)LoRA 微调适配器 | |
| `LICENSE` | 声明许可协议 | |
| `MESSAGE` | 预置多轮对话历史 | |

### 常用 PARAMETER

| 参数 | 说明 | Agent 场景建议 |
|---|---|---|
| `temperature` | 采样温度（0~2），越高越随机 | 工具调用 0~0.3，代码 0.1~0.3 |
| `top_p` | 核采样（0~1） | 0.8~0.9 |
| `num_ctx` | 上下文长度（token） | 按需，见[第4章](../C4) |
| `num_gpu` | 卸载到 GPU 的层数（-1 全部） | 见 [C2.5](../C2/5.%20GPU-CPU%20混合推理配置.md) |
| `stop` | 停止符列表 | 与模板配套 |
| `repeat_penalty` | 重复惩罚 | 默认即可 |

## 三、实战：构建一个「Agent 助手」模型

### 3.1 方式一：CLI + Modelfile 文件（传统方式）

创建 `Modelfile` 文件：

```dockerfile
FROM qwen3:8b

# 固化 Agent 角色的系统提示词
SYSTEM """你是一个严谨的本地 Agent 助手。
规则：
1. 回答简洁准确，使用中文；
2. 需要外部信息时，直接发起工具调用，不要解释你要做什么；
3. 工具返回结果后，基于结果给出最终答案。"""

# 固化采样参数：低温度保证工具调用稳定
PARAMETER temperature 0.2
PARAMETER top_p 0.8
```

构建并验证：

```bash
ollama create qwen3-agent -f ./Modelfile
ollama list                 # 可以看到 qwen3-agent
ollama run qwen3-agent      # 直接使用
ollama show qwen3-agent --modelfile   # 查看构建出的 Modelfile
```

### 3.2 方式二：REST API（0.17.x 结构化字段，实测通过）

Ollama 0.17.x 的 `/api/create` 已支持结构化字段（不再需要手写 Modelfile 文本），本书实测环境（Ollama 0.17.7 + qwen3:8b）验证成功：

```bash
curl http://localhost:11434/api/create -d '{
  "model": "qwen3-agent-demo",
  "from": "qwen3:8b",
  "system": "你是一个严谨的本地 Agent 助手，回答问题简洁准确，需要调用工具时直接调用。",
  "parameters": {"temperature": 0.2, "top_p": 0.8}
}'
```

返回流式状态，最后一条为 `{"status":"success"}`。

验证构建结果：

```bash
curl http://localhost:11434/api/show -d '{"model": "qwen3-agent-demo"}'
```

实测返回（节选）：

```json
{
  "system": "你是一个严谨的本地 Agent 助手，回答问题简洁准确，需要调用工具时直接调用。",
  "parameters": {
    "temperature": 0.2,
    "top_p": 0.8
  },
  "model_info": { "general.architecture": "qwen3" }
}
```

> 不需要的演示模型可用 `ollama rm qwen3-agent-demo` 清理。

### 3.3 方式三：Python SDK

```python
import ollama

resp = ollama.create(
    model="qwen3-agent",
    from_="qwen3:8b",
    system="你是一个严谨的本地 Agent 助手……",
    parameters={"temperature": 0.2},
)
print(resp.status)  # success
```

## 四、TEMPLATE：进阶定制（可选）

`TEMPLATE` 决定「系统提示词、对话历史、工具描述」如何拼装成发送给模型的完整文本。Ollama 为每个模型家族内置了默认模板（Qwen 系用 ChatML 格式），**绝大多数场景不需要改**。

只有当你熟悉目标模型的提示词格式、需要特殊注入（如自定义工具协议）时才使用：

```dockerfile
FROM qwen3:8b
TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ range .Messages }}<|im_start|>{{ .Role }}
{{ .Content }}<|im_end|>
{{ end }}<|im_start|>assistant
"""
```

> ⚠️ TEMPLATE 写错会导致模型行为异常（如工具调用失效）。修改前先用 `ollama show <模型> --modelfile` 查看原模板，小步修改、及时回滚。

## 五、MESSAGE：预置对话（少样本示例）

Agent 场景可以用 `MESSAGE` 预置几条「正确的工具调用示范」，帮助小模型学会调用模式：

```dockerfile
FROM qwen3:8b
MESSAGE user 北京今天天气怎么样?
MESSAGE assistant 我需要查询天气工具。{"city": "北京"}
```

## 本章小结

- Modelfile 是模型定制的事实标准，指令：FROM / PARAMETER / SYSTEM / TEMPLATE / ADAPTER / LICENSE / MESSAGE；
- **Agent 场景推荐固化：角色系统提示词 + 低温度参数**，团队共享"调好参的模型"；
- 三种构建方式：CLI `ollama create -f`、REST API 结构化字段（0.17.x 实测）、Python SDK；
- TEMPLATE 属于高级操作，默认模板够用，改动需谨慎并测试。

## 延伸阅读

- 官方 Modelfile 参考：[https://docs.ollama.com/modelfile](https://docs.ollama.com/modelfile)
- 有效参数完整清单：[https://docs.ollama.com/modelfile#valid-parameters-and-values](https://docs.ollama.com/modelfile#valid-parameters-and-values)
